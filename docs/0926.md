按当前 `main` 最新版 `c5a732c`，这一轮不要再继续加抽象。直接把模型收敛成：

```text
Team        = 组织 / 权限边界
Member      = 长期存在的 Agent / “人”
Discussion  = 临时多人协作房间
Work        = 一个业务工作上下文，当前继续绑定 Jira
Direct      = 单聊
Execution   = 一次真正运行
```

**不要新增 `AgentTeam`、`Teammate`、`LeadAgent`、`work_task`。**

当前代码已经有 `WorkspaceNav / ConversationSidebar / TeamManagement / CapabilitySettings / MemberProfile Drawer / Global+Team Memory`，这些全部保留，不再继续拆成更多组件。

---

# 1. 后端：彻底取消 Member 自动接龙

这是这一轮最重要的代码改动。

## 1.1 完整重写 `server/group-dispatcher.ts`

整个文件改成下面这个版本：

```ts
import type {
  Conversation,
  ConversationMessage,
  Member,
  WakeReason,
} from './domain.js';
import type { ConversationMemberService } from './conversation-member-service.js';

/**
 * Group Dispatcher 只负责确定性收件人选择。
 *
 * 规则：
 *
 * direct / work
 *   -> 唯一 Member
 *
 * group + targetMemberId
 *   -> 指定 Member
 *
 * group + @mention
 *   -> 被 @ 的 Member
 *
 * group + 无 mention
 *   -> 用户消息：Everyone
 *   -> Member 消息：不自动唤醒任何人
 *
 * Member 之间的后续协作只能显式使用：
 *   message_member
 *   ask_member
 *
 * 不做自动 follow-up / open discussion / round limit / lead fallback。
 */
export class GroupDispatcher {
  constructor(
    private readonly states: ConversationMemberService,
  ) {}

  plan(input: {
    conversation: Conversation;
    message: ConversationMessage;
    authorMemberId?: string;
  }): DispatchPlan {
    const { conversation, message, authorMemberId } = input;

    const active = conversation.members.filter(
      (member) => member.status === 'active',
    );

    if (active.length === 0) {
      return {
        wakes: [],
        unresolvedMentions: [],
      };
    }

    // API 明确指定 targetMemberId。
    if (message.targetMemberId) {
      const target = active.find(
        (member) => member.id === message.targetMemberId,
      );

      if (!target) {
        return {
          wakes: [],
          unresolvedMentions: [],
        };
      }

      if (target.id === authorMemberId) {
        return {
          wakes: [],
          unresolvedMentions: [],
        };
      }

      if (this.states.get(conversation.id, target.id).muted) {
        return {
          wakes: [],
          unresolvedMentions: [],
        };
      }

      return {
        wakes: [
          {
            memberId: target.id,
            reason: 'direct',
            triggerSequence: message.messageSequence,
          },
        ],
        unresolvedMentions: [],
      };
    }

    // direct / work：只有一个 Member。
    if (conversation.kind !== 'group') {
      const target = active[0];

      if (!target || target.id === authorMemberId) {
        return {
          wakes: [],
          unresolvedMentions: [],
        };
      }

      if (this.states.get(conversation.id, target.id).muted) {
        return {
          wakes: [],
          unresolvedMentions: [],
        };
      }

      return {
        wakes: [
          {
            memberId: target.id,
            reason: 'direct',
            triggerSequence: message.messageSequence,
          },
        ],
        unresolvedMentions: [],
      };
    }

    // group：先处理 @mention。
    const { matched, unresolved } = resolveMentions(
      message.content,
      active,
    );

    if (matched.length > 0) {
      const targets = matched
        .filter((member) => member.id !== authorMemberId)
        .filter(
          (member) =>
            !this.states.get(conversation.id, member.id).muted,
        );

      return {
        wakes: targets.map((member) => ({
          memberId: member.id,
          reason: 'mention',
          triggerSequence: message.messageSequence,
        })),
        unresolvedMentions: unresolved,
      };
    }

    // 明确 @ 了不存在的人，不广播。
    if (unresolved.length > 0) {
      return {
        wakes: [],
        unresolvedMentions: unresolved,
      };
    }

    // Member 发言，不自动接龙。
    if (message.senderType === 'member') {
      return {
        wakes: [],
        unresolvedMentions: [],
      };
    }

    // 用户没有 @，就是 Everyone。
    const targets = active
      .filter((member) => member.id !== authorMemberId)
      .filter(
        (member) =>
          !this.states.get(conversation.id, member.id).muted,
      )
      .filter(
        (member) =>
          this.states.get(conversation.id, member.id)
            .lastSeenMessageSequence <
          message.messageSequence,
      );

    return {
      wakes: targets.map((member) => ({
        memberId: member.id,
        reason: 'everyone',
        triggerSequence: message.messageSequence,
      })),
      unresolvedMentions: [],
    };
  }
}

export interface WakePlan {
  memberId: string;
  reason: Exclude<WakeReason, 'schedule'>;
  triggerSequence: number;
}

export interface DispatchPlan {
  wakes: WakePlan[];
  unresolvedMentions: string[];
}

const MENTION_PATTERN =
  /@([^\s@,，。；;：:！!？?、()（）[\]【】<>《》"'`]+)/g;

export function resolveMentions(
  content: string,
  members: Member[],
): {
  matched: Member[];
  unresolved: string[];
} {
  const matched = new Map<string, Member>();
  const unresolved: string[] = [];

  const byHandle = new Map<string, Member>();
  const byName = new Map<string, Member>();

  for (const member of members) {
    const handle = member.handle.trim().toLowerCase();

    if (handle && !byHandle.has(handle)) {
      byHandle.set(handle, member);
    }

    for (const key of [
      member.name,
      member.name.replace(/\s+/g, ''),
    ]) {
      const normalized = key.trim().toLowerCase();

      if (normalized && !byName.has(normalized)) {
        byName.set(normalized, member);
      }
    }
  }

  for (const match of content.matchAll(MENTION_PATTERN)) {
    const raw = match[1];
    const token = raw.toLowerCase();

    const hit = byHandle.get(token) ?? byName.get(token);

    if (hit) {
      matched.set(hit.id, hit);
    } else {
      unresolved.push(raw);
    }
  }

  return {
    matched: [...matched.values()],
    unresolved: [...new Set(unresolved)],
  };
}
```

---

# 2. `WakeReason` 改成 4 种

## `server/domain.ts`

把现在：

```ts
export type WakeReason =
  | 'direct'
  | 'mention'
  | 'open_discussion'
  | 'follow_up'
  | 'schedule';
```

改成：

```ts
export type WakeReason =
  | 'direct'
  | 'mention'
  | 'everyone'
  | 'schedule';
```

不要再出现：

```text
open_discussion
follow_up
```

---

# 3. 删除自动接龙

## `server/team-service.ts`

在 Member turn 完成以后，现在有：

```ts
if (!isMemberDm(input.conversation)) {
  this.dispatchMessage({
    conversation: input.conversation,
    message,
    authorMemberId: input.member.id,
  });
}
```

**整个删除。**

保留 Member 消息落库：

```ts
const message = this.insertMemberMessage({
  conversationId: input.conversation.id,
  memberId: input.member.id,
  content: outcome.content,
  executionId,
  replyToMessageId: null,
});

this.states.markSeen(
  input.conversation.id,
  input.member.id,
  context.consumedThroughSequence,
);

this.states.markReplied(
  input.conversation.id,
  input.member.id,
  message.messageSequence,
);

this.updateExecution(executionId, {
  status: 'completed',
  decision: 'reply',
  response: outcome.content,
  endedAt: now(),
});

this.emit(input.conversation.id, {
  type: 'message.created',
  data: message,
});

this.emitExecution(this.getExecution(executionId));
this.touchConversation(input.conversation.id);
this.touchAgentPresence(input.member.id);
```

到这里就结束。

于是：

```text
User → Everyone
    ↓
A
B
C

A 回复
    ↓
不会自动唤醒 B/C
```

Agent 想继续合作：

```text
ask_member
message_member
```

---

# 4. `GroupDispatcher` 的构造函数同步修改

## `server/team-service.ts`

当前：

```ts
this.dispatcher = new GroupDispatcher(
  db,
  this.states,
  config.groupAutoWakeRounds,
);
```

改成：

```ts
this.dispatcher = new GroupDispatcher(this.states);
```

---

# 5. 删除自动唤醒配置

## `server/config.ts`

删除整个：

```ts
groupAutoWakeRounds: intEnv(
  'GROUP_AUTO_WAKE_ROUNDS',
  2,
),
```

以及前面的相关注释。

## `.env.example`

删除：

```env
GROUP_AUTO_WAKE_ROUNDS=2
```

## `README.md`

全局搜索：

```text
GROUP_AUTO_WAKE_ROUNDS
groupAutoWakeRounds
follow_up
open_discussion
```

全部删掉。

---

# 6. ContextAssembler 改成三种 group 行为

## `server/context-assembler.ts`

找到 `discussionInstruction()`，完整替换：

```ts
function discussionInstruction(
  reason: WakeReason | null,
): string {
  if (reason === 'mention') {
    return [
      'You were addressed by name, so you must respond.',
      'Do not repeat what other participants already said.',
      'Add only information that is useful and distinct.',
      'Reply directly and concisely.',
    ].join('\n');
  }

  if (reason === 'everyone') {
    return [
      'You are one of the participants included in this discussion.',
      '',
      'Decide whether you have something useful to contribute.',
      `If you do not have anything useful or non-duplicative to add, reply with exactly: ${NO_REPLY_SENTINEL}`,
      '',
      'Do not summarize the entire discussion.',
      'Do not agree merely for the sake of participating.',
      'Add information, reasoning, risks, or a concrete next step only when useful.',
    ].join('\n');
  }

  return [
    'Reply directly to the user.',
    'Be concise and useful.',
  ].join('\n');
}
```

最终：

```text
direct
    必须回答

mention
    必须回答

everyone
    可以 NO_REPLY

schedule
    按 direct/work 执行
```

---

# 7. 不要新增 `AgentTeam`

这个非常明确：

## 不新增

```text
server/agent-team-service.ts
server/agent-team-service.ts
agent_team
team_session
teammate
lead_member_id
```

也不要改：

```text
conversation.kind
```

继续：

```text
direct
group
work
```

其中：

```text
group = 临时协作讨论
```

而不是：

```text
group = Team entity
```

---

# 8. 解决当前最明显的概念错误：“New Team”

现在 UI 的：

```text
New Team
```

实际上调用的是：

```text
POST /api/conversations
kind = group
```

所以这根本不是创建 Team。

必须改名。

## `src/components/team/ConversationList.tsx`

```text
New Team
```

改成：

```text
New discussion
```

## `src/components/team/GroupCreator.tsx`

标题：

```text
New Team
```

改：

```text
New discussion
```

按钮：

```text
Create Team
```

改：

```text
Create discussion
```

错误提示：

```text
Team 至少两个成员
```

改：

```text
Discussion 至少需要两个 Member
```

---

# 9. Conversation 分类也改掉

## `src/components/team/ConversationList.tsx`

现在：

```ts
{ title: 'Team discussions', ... }
```

改：

```ts
{ title: 'Discussions', items: visible.filter((c) => c.kind === 'group') },
{ title: 'Work', items: visible.filter((c) => c.kind === 'work') },
{ title: 'Direct', items: visible.filter((c) => c.kind === 'direct') },
```

`conversationTag()`：

```tsx
if (isMemberDm(conversation)) {
  return <Tag color="purple">private</Tag>;
}

if (conversation.kind === 'group') {
  return <Tag color="blue">discussion</Tag>;
}

if (conversation.kind === 'work') {
  return <Tag color="gold">work</Tag>;
}

return <Tag>direct</Tag>;
```

---

# 10. 不要加 `work_task`

这一轮明确不做：

```text
work_task
task_service
task dependency
task claim
```

原因是你现在已经决定：

```text
Work
  ↓
Jira = business truth
```

再在本地做一份 Task system，会重新制造：

```text
Jira task
+
local task
```

两个事实源。

当前先保持：

```text
Work
├── Conversation
├── Jira reference
└── Execution
```

Agent 之间需要拆任务：

```text
ask_member
```

就够了。

以后真的需要 Task，优先映射 Jira Subtask，而不是再建 `work_task`。

---

# 11. UI：不要再继续拆更多 Workspace 组件

当前已经有：

```text
WorkspaceNav
ConversationSidebar
TeamManagement
CapabilitySettings
MemberProfile
```

**不要再新增：**

```text
WorkspaceShell
ChatWorkspace
ConversationView
TeamWorkspace
SettingsWorkspace
```

当前 `TeamChat.tsx` 虽然名字不漂亮，但它现在已经是整个页面 controller。

直接把它改名即可。

## 文件重命名

```text
src/components/TeamChat.tsx
```

→

```text
src/components/Workspace.tsx
```

里面：

```ts
export function Workspace()
```

## `src/App.tsx`

改：

```ts
import { Workspace } from './components/Workspace';
```

然后：

```tsx
<Content style={{ minHeight: 0 }}>
  <Workspace />
</Content>
```

这样名字终于与职责一致。

---

# 12. New Discussion / New Work 改成 Modal

这是当前 UI 最值得改的一点。

现在：

```text
ConversationSidebar
  ↓
GroupCreator Card
```

和：

```text
ConversationSidebar
  ↓
WorkCreator Card
```

导致左侧越来越长。

改为：

```text
ConversationSidebar
  ↓
New button
```

然后 Modal 出现在 Workspace 根部。

---

# 13. `ConversationSidebar.tsx`

把这些 props：

```ts
showGroupCreator
onToggleGroupCreator
onCancelGroupCreator
showWorkCreator
onToggleWorkCreator
onCancelWorkCreator
```

全部删除。

改成：

```ts
interface ConversationSidebarProps {
  members: Member[];
  conversations: Conversation[];
  selectedConversationId: string | null;
  onSelectConversation: (conversationId: string) => void;
  onNewDiscussion: () => void;
  onNewWork: () => void;
}
```

底部改成：

```tsx
<Space direction="vertical" style={{ width: '100%', marginTop: 8 }}>
  <Button
    type="primary"
    block
    icon={<PlusOutlined />}
    onClick={onNewDiscussion}
  >
    New discussion
  </Button>

  <Button
    block
    icon={<ProjectOutlined />}
    onClick={onNewWork}
  >
    New work
  </Button>
</Space>
```

不要再把 Form/Card 塞进第二列。

---

# 14. `GroupCreator.tsx` 改成 Modal

Props 改：

```ts
interface GroupCreatorProps {
  open: boolean;
  members: Member[];
  onCreate: (input: {
    title: string;
    memberIds: string[];
  }) => Promise<void>;
  onCancel: () => void;
}
```

返回：

```tsx
<Modal
  open={open}
  title="New discussion"
  okText="Create discussion"
  cancelText="Cancel"
  onCancel={onCancel}
  onOk={() => void submit()}
  okButtonProps={{
    disabled: !canCreate,
    loading: busy,
  }}
>
  <Space direction="vertical" style={{ width: '100%' }} size={12}>
    <Input
      value={title}
      onChange={(e) => setTitle(e.target.value)}
      placeholder="例如：Security architecture review"
      autoFocus
    />

    <div>
      <Typography.Text type="secondary">
        Participants
      </Typography.Text>

      <Checkbox.Group
        value={selected}
        onChange={(values) =>
          setSelected(values as string[])
        }
        style={{
          width: '100%',
          marginTop: 8,
        }}
      >
        <Space
          direction="vertical"
          style={{ width: '100%' }}
        >
          {members.map((member) => (
            <Checkbox key={member.id} value={member.id}>
              {member.name}
              <Typography.Text type="secondary">
                {' '}· {member.role}
              </Typography.Text>
            </Checkbox>
          ))}
        </Space>
      </Checkbox.Group>
    </div>

    {selected.length < 2 && (
      <Alert
        type="info"
        showIcon
        message="至少选择 2 个 Member。"
      />
    )}

    {error && (
      <Alert
        type="error"
        showIcon
        message={error}
      />
    )}
  </Space>
</Modal>
```

这样不会再把整个左栏撑长。

---

# 15. `WorkCreator.tsx` 同样改 Modal

Props 增加：

```ts
open: boolean;
```

返回：

```tsx
<Modal
  open={open}
  title="New work"
  okText={instruction.trim() ? 'Create and start' : 'Create'}
  cancelText="Cancel"
  onCancel={onCancel}
  onOk={() => void submit()}
  confirmLoading={busy}
  okButtonProps={{
    disabled: !canCreate,
  }}
>
```

字段保持：

```text
Title
Member
Jira issue
Instruction
```

删除这段大 Alert：

```text
Current Work 里暂时不会出现……
```

改成一句小的：

```tsx
<Typography.Text type="secondary">
  留空 instruction 只创建 Work；填写后会立即开始执行。
</Typography.Text>
```

---

# 16. `Workspace.tsx` 增加两个 Modal state

把现在：

```ts
type CreatorKind = 'member' | 'group' | 'work' | null;
const [creator, setCreator] = useState<CreatorKind>(null);
```

拆掉。

改成：

```ts
const [newDiscussionOpen, setNewDiscussionOpen] =
  useState(false);

const [newWorkOpen, setNewWorkOpen] =
  useState(false);
```

Member 新建仍然是 Team Management 里的独立状态：

```ts
const [newMemberOpen, setNewMemberOpen] =
  useState(false);
```

这样三个创建窗口互不干扰。

---

# 17. `Workspace.tsx` 的 Sidebar 调用

改成：

```tsx
<ConversationSidebar
  members={members}
  conversations={conversations}
  selectedConversationId={conversationId}
  onSelectConversation={openConversation}
  onNewDiscussion={() => setNewDiscussionOpen(true)}
  onNewWork={() => setNewWorkOpen(true)}
/>
```

---

# 18. Workspace 最底部统一放两个 Modal

在 `<Layout>` 内或最外层：

```tsx
<GroupCreator
  open={newDiscussionOpen}
  members={members}
  onCreate={createGroup}
  onCancel={() => setNewDiscussionOpen(false)}
/>

<WorkCreator
  open={newWorkOpen}
  members={members}
  onCreate={createWork}
  onCancel={() => setNewWorkOpen(false)}
/>
```

---

# 19. `createGroup()` 不再操作 Creator

现在：

```ts
setCreator(null);
```

全部删除。

改：

```ts
async function createGroup(input: {
  title: string;
  memberIds: string[];
}) {
  const result = await api.createConversation({
    kind: 'group',
    title: input.title,
    memberIds: input.memberIds,
  });

  setConversations((current) => [
    result.conversation,
    ...current.filter(
      (item) => item.id !== result.conversation.id,
    ),
  ]);

  setNewDiscussionOpen(false);
  openConversation(result.conversation.id);
  setView('chat');
}
```

---

# 20. `createWork()` 同样

删除：

```ts
setCreator(null);
```

改：

```ts
setNewWorkOpen(false);
```

其它逻辑保持。

---

# 21. Conversation Header：只负责“识别房间”

现在 Header 里面有：

```text
Members button
recipient select
avatar 点击 = mute
GroupMemberManager inline
```

这太杂。

改成只：

```text
Title
kind / Jira
avatars
participants button
```

---

# 22. `ConversationHeader.tsx` Props 改掉

删除：

```ts
recipientMemberId
onRecipientChange
onToggleMute
showMembers
onToggleMembers
```

保留：

```ts
interface ConversationHeaderProps {
  conversation: Conversation;
  allMembers: Member[];
  states: Record<string, ConversationMemberState>;
  memberStatus: MemberStatusLookup;
  onConversationChanged: (conversation: Conversation) => void;
  onStateChanged: (state: ConversationMemberState) => void;
}
```

---

# 23. Header 改成这个结构

```tsx
export function ConversationHeader({
  conversation,
  allMembers,
  states,
  memberStatus,
  onConversationChanged,
  onStateChanged,
}: ConversationHeaderProps) {
  const isGroup = conversation.kind === 'group';
  const isDm = isMemberDm(conversation);
  const [participantsOpen, setParticipantsOpen] = useState(false);

  return (
    <>
      <div className="conversation-header">
        <div className="conversation-header-main">
          <Space size={8} align="center">
            <Typography.Title
              level={4}
              style={{ margin: 0 }}
            >
              {conversation.title}
            </Typography.Title>

            {conversation.kind === 'work' && (
              <Tag color="gold">Work</Tag>
            )}

            {conversation.externalWorkRef?.key && (
              <Tag color="cyan">
                {conversation.externalWorkRef.key}
              </Tag>
            )}
          </Space>

          <Typography.Text type="secondary">
            {isDm
              ? conversation.members
                  .map((member) => member.name)
                  .join(' ↔ ')
              : isGroup
                ? `${conversation.members.length} participants`
                : conversation.members[0]?.name ?? ''}
          </Typography.Text>
        </div>

        <Space>
          <Avatar.Group
            max={{ count: 5 }}
          >
            {conversation.members.map((member) => {
              const status = memberStatus(member.id);

              const dot =
                status.className === 'working'
                  ? 'processing'
                  : status.className === 'muted'
                    ? 'default'
                    : 'success';

              return (
                <Tooltip
                  key={member.id}
                  title={`${member.name} · ${status.label}`}
                >
                  <Badge dot status={dot}>
                    <Avatar>
                      {member.name
                        .slice(0, 1)
                        .toUpperCase()}
                    </Avatar>
                  </Badge>
                </Tooltip>
              );
            })}
          </Avatar.Group>

          {isGroup && (
            <Button
              icon={<TeamOutlined />}
              onClick={() =>
                setParticipantsOpen(true)
              }
            >
              Participants
            </Button>
          )}
        </Space>
      </div>

      {isGroup && (
        <GroupMemberManager
          open={participantsOpen}
          conversation={conversation}
          allMembers={allMembers}
          states={states}
          onConversationChanged={onConversationChanged}
          onStateChanged={onStateChanged}
          onClose={() => setParticipantsOpen(false)}
        />
      )}
    </>
  );
}
```

**Avatar 点击不要再自动 mute。**

Mute 必须进入 Participants Drawer 明确操作。

---

# 24. `GroupMemberManager.tsx` 改成 Drawer

Props：

```ts
interface GroupMemberManagerProps {
  open: boolean;
  conversation: Conversation;
  allMembers: Member[];
  states: Record<string, ConversationMemberState>;
  onConversationChanged: (conversation: Conversation) => void;
  onStateChanged: (state: ConversationMemberState) => void;
  onClose: () => void;
}
```

最外层：

```tsx
<Drawer
  open={open}
  title="Participants"
  width={480}
  onClose={onClose}
>
```

把：

```text
Team Members
```

全部替换为：

```text
Participants
```

把：

```text
Mute
Remove
Add member
```

保留。

不要再出现：

```text
Lead
Primary
Team member
```

---

# 25. Recipient selector 从 Header 移到 MessageComposer

这一步会明显干净。

## `MessageComposer.tsx`

Props 增加：

```ts
recipientMemberId: string;
onRecipientChange: (memberId: string) => void;
```

Group 的 Sender：

```tsx
<Sender
  value={value}
  onChange={onChange}
  onSubmit={() => onSend()}
  loading={busy}
  disabled={disabled || readOnly}
  placeholder="对讨论说点什么… 使用 @handle 指定成员"
  submitType="enter"
  prefix={
    conversation.kind === 'group' ? (
      <Select
        size="small"
        bordered={false}
        value={recipientMemberId}
        onChange={onRecipientChange}
        options={[
          {
            value: EVERYONE,
            label: 'Everyone',
          },
          ...conversation.members
            .filter(
              (member) => member.status === 'active',
            )
            .map((member) => ({
              value: member.id,
              label: `@${member.handle}`,
            })),
        ]}
      />
    ) : undefined
  }
/>
```

这样 Header：

```text
谁在这里？
```

Composer：

```text
这条消息发给谁？
```

职责非常清楚。

---

# 26. `Workspace.tsx` 删除 Header 里的 recipient props

从：

```tsx
<ConversationHeader
  ...
  recipientMemberId={recipientMemberId}
  onRecipientChange={setRecipientMemberId}
  onToggleMute={...}
  showMembers={...}
  onToggleMembers={...}
/>
```

改：

```tsx
<ConversationHeader
  conversation={selectedConversation}
  allMembers={members}
  states={conversationStates}
  memberStatus={memberStatus}
  onConversationChanged={applyConversationChanged}
  onStateChanged={(state) =>
    applyStateChanged({
      memberId: state.memberId,
      state,
    })
  }
/>
```

然后 `MessageComposer`：

```tsx
<MessageComposer
  conversation={selectedConversation}
  value={input}
  onChange={setInput}
  onSend={() => void send()}
  busy={busy}
  disabled={!conversationId}
  recipientMemberId={recipientMemberId}
  onRecipientChange={setRecipientMemberId}
/>
```

---

# 27. `Workspace.tsx` 删除 showMemberManager

删除：

```ts
const [showMemberManager, setShowMemberManager] =
  useState(false);
```

以及：

```ts
setShowMemberManager(false);
```

---

# 28. Team 管理页保留当前结构，不再改

当前：

```text
Team
├── Members
├── Current Work
└── Automation
```

已经合理。

不用新增：

```text
TeamTasks
Projects
AgentTeams
```

---

# 29. `CurrentWorkSection.tsx` 保持

当前已经是：

```text
active execution
→ Current Work
```

保持。

不要创建：

```text
current_work table
```

也不要建立第二份 Work state。

---

# 30. Work 语义保持

当前：

```text
Work
= kind='work'
= 一个 Member
= 可挂 Jira
```

保持。

不要为了模仿 Claude 改成：

```text
Work
= 多 Member
= 新增 AgentTeam
```

多 Agent 协作使用：

```text
Discussion
```

或者：

```text
Work 内 Member 使用 ask_member
```

这已经够了。

---

# 31. Memory：保留现在的 Global + Team，但禁止 Agent 自动写 Global

当前 `server/member-memory.ts` 已经很好，保持：

```text
.data/members/<memberId>/
├── memory/MEMORY.md
└── teams/<teamId>/MEMORY.md
```

不用再改结构。

真正要改的是 `remember_member`。

---

# 32. `server/capabilities/providers/core-tools.ts`

把：

```ts
remember_member
```

参数从：

```ts
parameters: z.object({
  content: z.string().min(1).max(8000),
  scope: z
    .enum(['team', 'global'])
    .optional(),
}),
```

改成：

```ts
parameters: z.object({
  content: z
    .string()
    .min(1)
    .max(8000)
    .describe(
      'Information to remember for the current Team.',
    ),
}),
```

description：

```ts
description:
  'Save something this Member should remember for the current Team. ' +
  'Use this for Team workflows, relationships, project facts, and local working conventions. ' +
  'Do not use this as a global personality or preference store.',
```

执行：

```ts
execute: (context, args) =>
  this.host.rememberMember({
    memberId: context.memberId,
    teamId: context.teamId,
    content: String(args.content),
  }),
```

---

# 33. `CoreToolHost` 同步简化

现在：

```ts
rememberMember(input: {
  memberId: string;
  teamId: string;
  content: string;
  scope: 'team' | 'global';
}): Promise<string>;
```

改：

```ts
rememberMember(input: {
  memberId: string;
  teamId: string;
  content: string;
}): Promise<string>;
```

---

# 34. `server/team-service.ts` 的 `rememberMember()`

现在：

```ts
rememberMember(input: {
  memberId: string;
  content: string;
  scope?: MemberMemoryScope;
  teamId?: string;
}): Promise<string> {
  if (input.scope === 'global') {
    return Promise.resolve(
      this.members.appendMemory(
        input.memberId,
        input.content,
      ),
    );
  }

  const teamId = input.teamId ?? this.defaultTeam().id;

  if (this.structure) {
    this.structure.getTeam(teamId);
  }

  return Promise.resolve(
    this.members.appendTeamMemory(
      input.memberId,
      teamId,
      input.content,
    ),
  );
}
```

改成：

```ts
rememberMember(input: {
  memberId: string;
  teamId: string;
  content: string;
}): Promise<string> {
  const teamId = input.teamId;

  if (this.structure) {
    this.structure.getTeam(teamId);
  }

  return Promise.resolve(
    this.members.appendTeamMemory(
      input.memberId,
      teamId,
      input.content,
    ),
  );
}
```

然后删除：

```ts
import type { MemberMemoryScope } from './member-memory.js';
```

---

# 35. Global Memory 改成“人工稳定人格”

## `src/components/team/MemberMemory.tsx`

Global tab 文案改：

```text
这段内容跨所有 Team。

只放这个 Member 长期稳定的工作习惯、
偏好和原则。

Agent 不会自动修改这里。
```

Team tab：

```text
这段内容只属于当前 Team。

用于记录当前 Team 的工作方式、
成员关系、项目事实和长期上下文。

Agent 可以在工作过程中记住这里的内容。
```

这样才真正实现：

```text
本性难移
+
环境不同
```

---

# 36. Member Profile 继续保持现在四个 Tab

当前已经是：

```text
Profile
Memory
Team Context
Skills
```

保留。

`MemberActivity` 也保留。

这已经解决：

```text
这个 Member 去过哪些 Team？
参与过哪些 conversation？
```

不用再增加 `relationship` / `persona` 表。

---

# 37. Profile 里的 Personality / Style 继续人工控制

`MemberEditor.tsx` 保留：

```text
Role
Description
Personality / Style
System Prompt
Model
```

但 `Style` 的提示文字改得更明确：

```text
稳定人格 / 工作风格。

这个字段跨所有 Team。
不要填写当前项目、客户或 Team 的临时要求。
```

---

# 38. `MemberActivity` 加一个简单的 Team 分组即可

当前已经显示：

```text
Conversation
Team
kind
time
```

不用新增数据库。

只把 UI 从：

```text
Recent activity (N)
```

改成：

```text
Recent activity
```

下面按 Team 分组：

```text
Financial Security
  Security architecture review
  Client access review

Platform Architecture
  Agent sandbox review
```

数据还是：

```text
GET /api/members/:id/conversations
GET /api/members/:id/teams
```

无需新表。

---

# 39. DB：删除 Lead

## `server/db-migrations.ts`

当前：

```ts
export const SCHEMA_VERSION = 16;
```

改成：

```ts
export const SCHEMA_VERSION = 17;
```

`conversation_member_state` 删除：

```sql
is_lead INTEGER NOT NULL DEFAULT 0,
```

删除：

```sql
CREATE UNIQUE INDEX idx_conversation_member_state_lead
  ON conversation_member_state(conversation_id)
  WHERE is_lead = 1;
```

因为项目当前明确规定：

```text
schema 改动 = 删除本地 DB 重建
```

所以不用写 migration。

---

# 40. `server/conversation-member-service.ts`

删除：

```text
isLead
setLead()
```

保留：

```text
lastSeenMessageSequence
lastRepliedMessageSequence
wakeStatus
pendingWake
pendingWakeTriggerSequence
pendingWakeReason
muted
updatedAt
```

这里不必继续折腾 `lastRepliedMessageSequence`，虽然新的 Dispatcher 不再使用它，但是它不是现在的大复杂度来源。

为了降低本轮风险，先保留。

下一轮再清理 dead field。

---

# 41. API 删除 Member Lead 方法

检查 `src/lib/api.ts`：

当前最新版已经只保留：

```ts
setMemberState(... { muted })
setMemberMuted(...)
```

所以这里无需增加任何 Lead API。

服务端如果还有：

```text
setMemberLead
```

直接删除。

---

# 42. `ConversationHeader` 中不要用 Avatar 点击 mute

这是必须改的。

现在：

```tsx
onClick={() => onToggleMute(member.id)}
```

删除。

Avatar 只负责：

```text
显示人
显示状态
```

操作去：

```text Participants
```

---

# 43. Participant Drawer 中保留 Mute

最终：

```text
Participants

Security Reviewer
● working
[ Mute ]

Senior Architect
● idle
[ Mute ]

Senior Engineer
● idle
[ Remove ]
```

这样不会发生：

> 我只是点了一下头像，怎么这个 Agent 被静音了？

---

# 44. ConversationHeader 的文案改掉所有 “Team”

Group conversation：

```text
discussion
```

Participant：

```text
Participants
```

不要：

```text
Team Members
New Team
Team discussion
```

真正的 Team 只出现在：

```text
Team workspace
Team management
Team context
```

---

# 45. Workspace Navigation 最终三个入口

当前的：

```text
Chat
Team
Settings
```

继续保留。

但是 Tooltip 改短：

```text
Chat
Team
Settings
```

不要：

```text
Chat（日常对话）
Team（成员 / Work / Schedules）
Settings（Capabilities）
```

视觉上更像正式产品。

---

# 46. Settings 继续使用当前 CapabilitySettings

当前：

```text
Company defaults
Team defaults
This member
```

不要重新设计 Capability backend。

保留：

```text
server/capabilities/catalog.ts
server/capabilities/registry.ts
server/capabilities/resolver.ts
server/capabilities/service.ts
```

继续保持：

```text
Skills
Knowledge
Actions
```

管理员完全不接触：

```text
providerId
selector
```

这部分已经修好了，不要再次重构。

---

# 47. Work 和 Discussion 的关系最终固定

用户行为：

```text
New discussion
    ↓
2+ Members
    ↓
多人讨论
```

或者：

```text
New work
    ↓
1 Member
    ↓
Jira / business work
```

Agent 要找另一个专家：

```text
ask_member
```

Agent 要问一下另一个人：

```text
message_member
```

不做：

```text
自动 follow-up
自动轮询
Lead
AgentTeam entity
```

---

# 48. `ask_member` 继续保留 blocking

不要把它改成普通 message。

保持：

```text
A
 ↓ ask_member
B
 ↓ execution
B result
 ↓
A resumes
```

数据库仍然记录：

```text
parent_execution_id
delegation_path
waiting_for_runtime_id
```

这部分很有价值。

---

# 49. `message_member` 继续建立真正的 1:1 Member conversation

继续：

```text
Member A
  ↔
Member B
```

这是 Cumora 风格长期协作里最有价值的一块。

不要再建：

```text
agent_mailbox
agent_message
teammate_channel
```

你现有的：

```text
conversation(kind=direct)
conversation_member
conversation_message
```

已经够了。

---

# 50. 一定要补这个语义测试

## `server/test/group-dispatcher.test.ts`

重写成：

```text
1. direct → sole member
2. work → sole member
3. group + targetMemberId → one member
4. group + @mention → mentioned members
5. group + unknown mention → nobody
6. group + user + no mention → everyone
7. group + member + no mention → nobody
8. muted member → not woken
9. member A reply does not automatically wake B
10. member A uses @B → B wakes
```

第 7、9 是这一轮最关键的两个测试。

---

# 51. Memory 测试

新增/修改 `server/test/member-memory.test.ts`：

```text
global memory visible in Team A
global memory visible in Team B

Team A memory visible in Team A
Team A memory absent in Team B

remember_member always writes Team memory

remember_member cannot choose global

human can still edit global memory
```

---

# 52. 删除这些测试

搜索：

```text
open_discussion
follow_up
groupAutoWakeRounds
GROUP_AUTO_WAKE_ROUNDS
pickPrimaryResponder
consecutiveMemberMessages
isLead
silent room
lead fallback
```

全部对应测试删除或改写。

---

# 53. README 的产品模型直接改成

加入这一段：

```md
## Core model

- **Team** — stable organizational and authorization boundary.
- **Member** — long-lived AI participant with stable identity and memory.
- **Discussion** — temporary multi-member conversation.
- **Work** — a focused business work context, optionally linked to Jira.
- **Direct** — one-to-one conversation.
- **Execution** — one concrete runtime turn.

A Discussion is not a Team entity.
A Member is not recreated for each Discussion.
Members do not automatically wake each other after every reply.
Agent-to-agent collaboration is explicit through `ask_member` and `message_member`.
```

---

# 54. 最终代码结构不要新增太多文件

最终保持现在的结构，只新增/修改少量：

```text
server/
├── group-dispatcher.ts        ← 简化
├── team-service.ts            ← 简化
├── domain.ts                  ← WakeReason
├── context-assembler.ts       ← discussion rules
├── conversation-member-service.ts
├── config.ts
├── db-migrations.ts
└── capabilities/providers/core-tools.ts

src/
├── App.tsx
├── components/
│   ├── Workspace.tsx           ← TeamChat 重命名
│   ├── workspace/
│   │   └── WorkspaceNav.tsx
│   ├── chat/
│   │   └── ConversationSidebar.tsx
│   └── team/
│       ├── ConversationHeader.tsx
│       ├── ConversationList.tsx
│       ├── GroupCreator.tsx
│       ├── WorkCreator.tsx
│       ├── GroupMemberManager.tsx
│       ├── MessageComposer.tsx
│       ├── MemberProfile.tsx
│       └── MemberMemory.tsx
```

**不新增：**

```text
ChatWorkspace.tsx
ConversationView.tsx
AgentTeamService.ts
AgentTeam.ts
WorkTaskService.ts
TaskBoard.tsx
Teammate.tsx
LeadAgent.tsx
PersonalityEngine.ts
```

---

# 55. 最终运行模型

代码最终应该形成：

```text
                         Team
                          │
             ┌────────────┴────────────┐
             │                         │
          Members                 Capabilities
             │
   ┌─────────┼─────────┐
   │         │         │
 Direct   Discussion   Work
             │          │
             │          └── Jira
             │
        Participants
             │
      ┌──────┴──────┐
      │             │
   Messages      Explicit delegation
      │             │
      │        ask_member
      │        message_member
      │
   Member Runtime
      │
 Copilot Session
```

Member 自身：

```text
Member
├── Identity
│   ├── role
│   ├── description
│   ├── style
│   └── systemPrompt
│
├── Global Memory
│   └── 人工维护的长期习惯
│
├── Team Memory
│   └── 当前 Team 的上下文
│
└── Skills
```

---

# 56. 这一轮明确“不做”的东西

为了 KISS，这几个全部不要做：

```text
AgentTeam entity
Lead Agent
自动 follow-up
自动轮流发言
open_discussion heuristic
Task table
Task dependency graph
Task claim system
Personality Engine
Relationship Engine
Conversation Persona
独立 Mailbox
独立 Agent Team Runtime
```

---

# 57. 最后按这个顺序提交

```text
commit 1
simplify group dispatcher
remove auto follow-up / lead

commit 2
rename group UI from Team → Discussion
move creator forms to Modal
clean ConversationHeader / Participants

commit 3
make Member global memory human-only
keep Team memory agent-writable

commit 4
rename TeamChat → Workspace
clean props / state wiring

commit 5
tests + schema 17
```

这版完成后，产品概念会非常稳定：

```text
Team       = 公司/团队组织
Member     = 长期存在的人
Discussion = 临时多人协作
Work       = 业务工作
Direct     = 私聊
Execution  = 一次运行
```

而 **Claude 的价值只留下“显式协作”这一个核心：`ask_member / message_member / 独立 runtime`；Cumora 的价值留下“Member 是长期同事、不是临时 teammate”这一层。** 其余自动协调机制和额外实体全部不引入。
