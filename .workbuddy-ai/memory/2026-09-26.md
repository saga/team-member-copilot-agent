# 2026-09-26

## External Work System Adapter（Pivot B 落地）

把「外部工作系统」从「裸 jira_issue_key + JiraClient 直调」升级成薄适配层。
核心决策：**本地没有 JiraIssue 这个业务对象**，只有 `ExternalWorkRef`（引用）
和 `ExternalWorkSnapshot`（execution 开始时向 Jira 取证的最小字段）。

### 新增

- `server/work-management/types.ts` — `ExternalWorkRef` / `ExternalWorkSnapshot`
  / `WorkManagementProvider`（search / get / addComment / transition / assign）
  / `WorkRefFactory`（命名，单独拆出来因为是身份问题不是操作问题）
  / `WorkManagementRegistry`（查不到就抛，不静默跳过校验+取证）+ 序列化/解析 helper。
  `normalizeExternalWorkRef` 只校验形状，**不查存在性** —— 建会话路径上不该有网络调用。
- `server/work-management/jira-provider.ts` — `JiraProvider` 包住既有 `JiraClient`。
  换 MCP 传输时新写一个实现、装配处换一行，业务契约不动。
- `server/routes/work-management.ts` — `GET /providers` + `POST /jira/webhook`。
  webhook 做**最小投影**：按 key 或不可变 id 找房间 → 发 `external_work.changed`
  （payload 只有 `{ref, changedFields, receivedAt}`）。zod schema 刻意不声明
  `issue.fields`，未知键被剥掉 —— 「不复制工单」由解析器强制，不靠人记得。
  共享密钥用 `timingSafeEqual`；空 = 无门禁（与 internal/admin token 同一约定）。
- `server/test/work-management.test.ts`（22 例）
- `scripts/smoke-work-management.mjs` — 拉起真实 `dist-server` 的 smoke test
  （单测自建 express，验不到 app.ts 的装配）。

### 改动

- schema v12 → **v13**：`conversation.external_work_ref` 取代 `jira_issue_key`；
  `execution` 加 `external_work_ref` + `external_work_snapshot`，去掉 `jira_issue_key`。
  表达式索引 `json_extract(external_work_ref,'$.key')` 供 webhook 反查
  （SQLite 3.51，planner 确实走索引）。
- `TeamService`：`captureWorkSnapshot()` 在 runTurn 里**尽力而为**地取证 ——
  失败只 warn、不废掉这一轮（外部系统可用性不该变成平台的可用性）。
  `updateExecution` 里快照写一次就不再被覆盖。
- `JiraToolProvider` 改成吃 `WorkManagementProvider`（版本 1 → 2）；
  控制面与 Agent **共用同一个 Provider 实例**。`assign` 在 Provider 上有、
  但**刻意不暴露成工具**（改负责人是责任转移，要人指定 accountId）。
- 清掉 `projectId` / `workItemId` 残留：`src/lib/api.ts`、ConversationList /
  TeamSidebar / ConversationHeader / TeamSections、若干注释。

### 顺手修掉的两个真问题

1. `scripts/mutation-check.py` 有 **6 条变异打不上**（引用的 `claim` / `work_item_id`
   代码已被删）+ 3 条「断言抓不住」。前者删掉、后者补回断言。现在 **28/28 全绿**
   （改之前是 9 条不成立）。
2. `manifestHashOf` 的 tools 段**没记 Provider version**，而 `domain.ts` 明写
   `capabilityManifestHash` 覆盖「skill / knowledge / tool 三层的组成与版本」。
   补上 —— 否则「换了 tool provider 实现、声明没变」在审计链上完全看不出来。

### 踩到的坑（重复踩过，务必记住）

- **BSD grep 的 `\|` 交替会静默返回空**，又一次误判成「这个 API 不存在」。
  一律用 Grep 工具或 `grep -E`。
- 测试里 `process.env.DATA_DIR` 必须在 `import('../config.js')` **之前**设好，
  所以 config 也得走 `await import()` —— 静态 import 会被提升到模块体之前。

### 未做

- Pivot A 遗留：`@handle` 前端寻址（仍走 `<select>`）、Agent DM = `direct` 会话、
  ExecutionTree / Cancel / Retry UI。
- Message 6 的 §1–§22 里，`PATCH /presence` 自助 vs admin、`PATCH /members` 等
  4 条路由的 `requireTeamMember()` 覆盖，仍未核实。

## 修 group 房间「用户提问、房间无人回答」（责任扩散）

用户截图：`arch review` 房间发「大家好」+「我希望做架构设计review，应该做什么」，
三个 Member **一条都没回**。查 `.data/team-member.db` 得到事实：6 条 execution
（3 人 × 2 条消息）全部 `status=completed`、`decision=skip`、`error=null`，
每条跑 10–14s。**机制完全正常，产品行为完全错误。**

根因是**责任扩散（bystander effect）**，不是模型偷懒：
`GroupDispatcher` 把用户消息以 `open_discussion` 广播给全体，而 prompt 告诉
**每个人**「没被点名 + 没东西可补 → 回 `<NO_REPLY>`」。三个人各自看都做了
合理判断，合起来是全体沉默。

### 修法：用户对着房间说话时，指定**唯一一名**应答者

- `GroupDispatcher.plan()`：`candidates` 算一次；`senderType === 'user'` 时
  `pickPrimaryResponder()` 选一人，其 `reason` 改成 `direct`，其余保持
  `open_discussion`。member 消息（follow_up）不指定 —— 顺带被唤醒的人才可以沉默。
- `pickPrimaryResponder()`：按 `last_replied_message_sequence` **升序**取第一
  （最久没发言的先答），平手按 `id` 定序。三条理由：判据是已有房间状态、重启后
  依然正确、天然轮流（不会永远同一个人回答）。**刻意不按 role 关键词猜**「谁最懂」
  —— 那是概率性路由，猜错了没法解释；要精确点名就用 `@handle`。
  `markReplied` 只在 `decision=reply` 分支调用（skip 分支提前 return），
  所以「最久没发言」是关于**发言**的事实，不是「被唤醒过」。
- `ContextAssembler.discussionInstruction()`：原来 `mustReply = direct || mention`
  共用一段文案。现在拆成三档 —— `mention`（用户点名）、`direct`（平台指定的
  应答者）、其余可沉默。**说辞必须分开**：告诉一个没被点名的人「用户点名了你」，
  模型可能先去纠正这个不存在的前提。`direct` 档显式写 "staying silent is not
  an option"，且**不出现** `<NO_REPLY>`。
- 关键：断言必须落在**渲染给模型的指令**上，不只是 `wakes` 里的 reason。
  reason 只是路由，真正改变行为的是模型读到的那段话。

### 验证

- `team-chat.test.ts` +5 例：恰好一名 `direct` / 全员静音时无应答者 /
  follow_up 不指定应答者 / 应答者与旁观者拿到的指令不同 / 选择规则（轮转 +
  平手定序 + 与成员数组顺序无关）。
  - 写第一版时断言「follow_up 的 member 不能是 alice」**失败了** ——
    提醒了一件事：**「作者不被自己的消息唤醒」是逐条消息的规则，不是逐个人的**。
    Alice 会被 Bob 的下一条消息合法唤醒。断言改成 JOIN 触发消息比 `sender_id`。
- mutation-check **28 → 32 条**全绿：新增「不指定应答者」「应答者拿到可沉默指令」
  「平手不定序」「排序方向反了」。
- 全量 225 例通过、typecheck、build、smoke 均通过。

### 顺带

- 删掉 `team-chat.test.ts` 里 `describe('Direct conversation', () => {})` 空壳
  （65f8615 删测试留下的残骸）。
- `turnModeFor` **不要**改成按 `wakeReason` 推 —— `runtime-reliability.test.ts:565`
  已把「group 房间一律 discussion 模式」写成契约（`direct` + discussion 并存是
  正常组合，`targetMemberId` 路径早就是这样）。

## 负责人兜底 + 哨兵不再泄漏（用户提的两件事）

用户原话：「1）是否可以指定 leader / key contact 这类比较资深的角色，如果大家都不
发言，这个人应该负责回答。符合职场文化。 2）no reply 不要显示了，然后再消失，
显得很诡异。」

### (1) 负责人（RoomLead）与兜底 escalation

- schema v13 → v14：`conversation_member_state.is_lead` + **偏索引**
  `idx_conversation_member_state_lead ON (conversation_id) WHERE is_lead = 1`。
  「一个房间至多一个负责人」做成**数据库不变量**而不是代码约定 —— 违反时是静默的、
  极难排查。跨行约束不能写成列 CHECK，偏索引才对。
- `ConversationMemberService.setLead()`：一条
  `is_lead = CASE WHEN member_id = ? THEN ? ELSE 0 END` 同时表达「指定这个」和
  「其余让位」，**没有零负责人/两个负责人的中间态**，所以不需要事务。
  通知要发**两行**（新上任 + 被顶掉的），只发前者会让旧负责人在 UI 上一直挂 Lead 徽章。
- `WakeReason` 加 `'escalation'`；`GroupDispatcher.planEscalation()` 只做花名册判断
  （kind=group / 有负责人 / 负责人在册且 active / 未被静音），返回 null 而不是抛错。
- `TeamService.maybeEscalateSilentRoom()`：在**每条** skip 收口**之后**检查这一批
  是否全体沉默。必须在收口之后（之前它自己算 active，「跑完没有」永远是否），
  也必须在每条之后（只有最后收口的那条能通过「全部收口」这一关）。
- 指令单独一档，且必须写出「房间里没人说话」—— 那是这一档独有的信息。
- **设计反转（重要）**：一开始加了个「负责人已经被明确问过就不再兜底」的守卫，
  理由是「同一件事不做两遍」。它让兜底在**最常见**的场景里失效（负责人恰好就是
  被指定的应答者、拿着「你必须回答」也选择了沉默）→ 房间继续沉默。删掉守卫，
  改成断言「这种情况**仍然**兜底」。房间保持沉默的代价没有上界，多跑一轮有。

### (2) `<NO_REPLY>` 不再流到客户端

- `member-decision.ts`：`couldStillBeNoReply()`（容忍 `normalize()` 的外壳：代码围栏、
  引号、结尾句号）+ `NoReplyStreamGate`（`push` 扣住仍可能是哨兵的前缀，一旦分叉
  立即放行、零额外延迟；`flush(decision)` **必须**带判定 —— skip 时那条尾巴就是哨兵本身）。
- 接线在 `runWake` 的 `onDelta`，`flush` 放在 `message.created` **之前**
  （那条事件会清掉流式占位，之后再补 delta 会留下没人收的占位）。

### 这轮真正抓到的 bug

- **`asWakeReason()` 没跟上新枚举值**。原来是
  `v === 'direct' || v === 'mention' || v === 'follow_up' ? v : 'open_discussion'`，
  加 `'escalation'` 时漏改了 → **最强**的唤醒原因读回来被降级成**最弱**的。
  影响面：`RecoveryService`（`findLostWakes`）重放一条 durable 的兜底唤醒时，负责人
  拿到「你可以沉默」，兜底在重启后静默失效 —— 而那正是它最该起作用的时候。
  修法：改成 `Record<Exclude<WakeReason,'schedule'>, true>` 白名单 + hasOwnProperty，
  漏写新值变成**编译错误**。字符串比较不会报错，Record 会。
  （`'toString' in obj` 会穿原型链，所以用 `Object.prototype.hasOwnProperty.call`。）
- **`createConversationSchema` 里还留着 Pivot B 之前的 `jiraIssueKey`**：前端发
  `externalWorkRef` 被 zod 静默剥掉，每个挂 Jira 的会话都返回 `externalWorkRef: null`，
  HTTP 201、任何地方都不报错。已补 `externalWorkRef` 声明 + 8 条真实 HTTP 回归测试
  （含 `provider` 用 `z.literal('jira')`，坏值走 400 而不是 500）。

### 测试/工具链上的教训

- **`message.delta` 刻意不落库**（token 级事件写库会爆），所以「哨兵有没有漏出去」
  **不能查库断言** —— 那是空绿。必须从活的 `team.subscribe()` 抓。
  同理 stub 必须**逐字符**喂 `onDelta`，整段一次性到达时过滤路径根本没被走到。
- **变异脚本必须给测试加超时**。拿掉「已经有人回答过就不再兜底」这一关会让兜底
  自我循环 → 测试不红而是永远跑下去 → 脚本挂死；而脚本是用 `finally` 还原文件的，
  被 kill 就会在磁盘上留下一个**改过的源文件**（本轮真的留下了一次，
  `group-dispatcher.ts` 的 `reason: 'escalation'` 变成 `'open_discussion'`，已修回）。
  现在 `TEST_TIMEOUT_SECONDS = 60`，超时即判定变异被捕获。
- **`setLead` 不需要事务**：单条 CASE UPDATE + 偏索引已经保证原子性，且 node:sqlite
  是同步 API、语句之间没有 await，不可能交错。注释里已写明「一次 UPDATE 比一次事务
  便宜」，别再把它「修」成多语句 + 事务。

### 验证

- 全量 **305 例**通过、typecheck、build 全绿。
- 变异验证 **32 → 45 条**：新增兜底降级 / 兜底不派 / 有人回答还兜底 / 不等跑完就兜底 /
  静音负责人也兜底 / member 沉默也兜底 / 兜底合并输给弱唤醒 / 兜底指令退化成 direct /
  `escalation` 没进读回白名单 / 换负责人不撤销旧的 / 读负责人不筛 `is_lead` /
  流式不过滤哨兵 / `flush` 不看判定。
- README 同步：Core model 加 `RoomLead` 行、§6 加「用户对着房间说话时房间欠一个回答」、
  §10 加「换负责人要发两行」、API 表更新 PATCH state、`schema v9` → `v14`、测试清单补齐。

### 变异验证抓到的覆盖漏洞（不是坏变异，是缺测试）

第一次跑 45 条变异，44 条变红，**第 36 条「不等这一批跑完就兜底」仍然全绿**。
原因不是断言写错，而是**没有能区分它的场景**：光看「一次沉默只兜一次」的计数，
拿掉 `active` 守卫后计数**仍然是对的** —— `escalations > 0` 那道守卫把后续重复派发
都拦掉了。计数对，**时机错**（兜底在第一个 skip 收口时就派出去了，那时还有人在想）。

修法：真的造出守卫所保护的中间态 ——「两条 skip 已收口、bob 还挂在 running 上」。
这需要 **按成员** 挂住 turn：全局 `hold` 会把所有人都按住，于是谁都不收口，
被考的那条分支根本走不到。给 stub 加了 `holdMemberIds: Set<string> | null`
（null = 按住全部，保持原行为）。补测后该变异变红。

教训：**变异仍然全绿时，先问「有没有能观察这条分支的形状」，再问「断言是不是写错了」。**

### 第二条空绿：SSE 上不能对原始字节做字符串匹配

补了一条「客户端真正看到的东西」的断言（真实 express + 真实 SSE）。第一版
`body.includes('NO_REPLY')` —— **永远为 false，包括哨兵真的漏出去的时候**。

原因：`message.delta` 是**逐字符**的，每个字符各自包在一帧 `data: {...}` 里，
所以 `<NO_REPLY>` 这十个字符在原始 SSE 字节流里**从来不会连续出现**。
（另外第一版还拿 `event: execution.updated` 当「流是活的」证据 —— 但 execution
建出来时也发这条事件，它在 delta **之前**，于是断言在 delta 到达前就通过了。）

修法：解析 SSE 帧 → 把 `message.delta` 的 `delta` 拼起来 → 断言拼出来的文字。
并且等**终态**事件到达客户端（它排在那一轮所有 delta 之后），用它区分
「没看到哨兵」和「还没读到」。改完两条变异都能抓到，报错信息就是用户看到的东西：
`哨兵漏到客户端了： "<NO_REPLY>"`。

顺带：`scripts/smoke-work-management.mjs` 里写死了 `schema v13` 和
`user_version === 13`，schema 升到 14 之后它会静默失效。已改成从
`dist-server/db-migrations.js` 读 `SCHEMA_VERSION` —— 版本号写死在这个脚本里
是必然过期的，而且没有任何东西会提醒你。

### 全量套件里两个**既有**的 flake（不是本轮改动引入的）

收尾跑全量时偶发 1 条红。先做了归因再动手：`git worktree add /tmp/tmca-head HEAD`
拉一份**干净的 HEAD** 跑 15 轮全量 → **6/15 失败**。所以是既有的，与本轮无关。

**1. `team-v1.test.ts` → Scheduler → "Missing expected exception"（HEAD 4/15）**

根因：测试里两处**独立读时钟**，而断言要求它们**精确相等**。

```ts
const past = new Date(Date.now() - 1000).toISOString();
const once = createScheduleDue({ ... });   // 它的默认参数又读了一次 Date.now()
...
assert.throws(() => structure.insertScheduleRun(once.id, past));  // 靠 UNIQUE 撞上
```

`toISOString()` 带毫秒，两次读只要跨一个毫秒边界就不相等 → `scheduled_for` 差 1ms
→ 没有 UNIQUE 冲突 → insert 成功 → 报 "Missing expected exception"。
`createScheduleDue` 本来就有 `dueAt` 参数，测试没传而已。修法是把 `past` 传进去。

单跑几乎不复现（无负载，两次调用贴在一起）；全量跑时中间夹着有负载的调度才容易跨边界。
修完：**60 轮全量 0 失败**（含精确归因）。

**2. `governance-p0.test.ts` → "配置 ADMIN_API_TOKEN 后…"（HEAD 2/15）**

同一份干净 HEAD 上还会红这一条（`config.adminApiToken` 是模块级单例，与并行度/负载相关）。
**本轮没有动它** —— 记录在此，留给后续排查。修完之后 20 轮全量 + 25 轮单跑都没再复现。

**教训（测量方法本身出错过一次）**：第一版用
`grep -q "once 只 fire 一次"` 判断失败属于哪条用例 —— 但**通过**的用例名字也会出现在
输出里，于是任何失败都被误判成 Scheduler 那条。改成抓 `^not ok`（顶层套件名）
或一个只有真失败才会打印的精确标记（`DBG past=`），归因才可信。
**归因统计必须只匹配失败输出，不能匹配整个输出。**

---

## 第二次删测试：307 → 257（用户决策，接受覆盖损失）

用户要求「再去掉 50 个 unit test，不重要的、冗余的、过时的、没必要的」。
选取原则沿用上一次（`65f8615`）：优先删「同文件存在更强兄弟测试」的（零信息损失），
其次参数化复制与琐碎断言。

### 删除构成（按文件）

- `capabilities` 5：`listProviders` 弱 getter、`默认能力` 两条（新对象 / ID 已注册）、
  `工具 implementation 变化 → 哈希变`、`工具声明的形状变化 → 哈希变`
- `work-management` 4：`缺 externalId 回落到 key`、`往返 serialize/parse`（连带删掉
  唯一使用 `serializeExternalWorkRef` 的 import）、`按引用找到 Provider`、
  `没有 issue 的 payload 回 200`
- `runtime-correctness` 2：`resume 未知错误 + 元数据也查不了`、`没绑定宿主工具：既不声明也不放行`
- `team-service` 2：`handle 冲突加后缀`、`group 移出成员后仍须满足形状约束`
- `data-integrity` 4：`不带 expectedVersion 强制覆盖`、`原子写不留临时文件`、
  `handle/name 撞车时 handle 赢`、`同一个 Member 被 @ 多次只算一次`
- `member-template-seeder` 3：`手工创建 Member seedKey 为 null`（形状用例是它的超集，
  连带改掉形状用例里「上面那条共存用例」的交叉引用）、`模板目录不存在`、`enabled: false`
- `knowledge-provider` 3：`listSources 报出指向哪个源`、`ensure 幂等`、`FTS 引号不炸`
- `runtime-reliability` 3：`runtime 单写者并发两轮`、`委派期间父 execution waiting_for_member`、
  `回放连续无洞 1..N`（`REPLAY_BATCH = 500`，它其实没有真的翻页 —— 与 durable sequence 用例同命题）
- `team-v1` 3：`默认 Team 唯一`、`空 key 的引用`、`webhook 最小投影`（与 work-management 逐字重复）
- `tool-policy` 3：`privileged 拒绝不依赖部署开关`、整个 `external-write 的放行权` describe（2 条）
- `internal-api` 2：`读留在了 Human API`、`未配置 token 时放行`（被「长在 /api/internal 下」超集）
- `member-dm` 2：`空内容不留空房间`、`归档 Member 不能建私聊`
- `governance-p0` 2：`启动校验未知 Provider`、`PolicyService 放行才允许`（与 tool-policy 逐字重复）
- `member-skills` 1：`非法 skill 名`
- `member-decision` 9：`skip` 表 4 行（`。` / ```` ```text ```` / `'…'` / `` `…` `` 四个包装变体）、
  `reply` 表 1 行、`stillPossible` 表 3 行（`<N` / `<NO_REP` / ```` ```js ````）、
  以及 `常量本身没有被改动`（魔数检查 —— 上一次也是按这条删的 `member-skills 魔数检查`）

### 变异验证抓到了 2 处**误伤**（必须记下来）

第一次跑 `scripts/mutation-check.py` → **2 条变异存活**：

- `manifest 对 skill 不做排序` ← 唯一杀手是被删掉的 `binding 顺序不影响哈希`
- `manifest 不记 Provider 版本` ← 唯一杀手是被删掉的 `Provider 实现版本变化 → 哈希变`

这两条不是「弱断言」，而是**真回归的唯一守卫**，所以**恢复**它们（并把
`manifestRegistry(overrides)` 的覆写参数一起恢复）。为保持 50 这个数字，改从
**变异脚本没有引用的文件**里补删 2 条：`member-decision` 的魔数检查 +
`governance-p0` 里与 tool-policy 逐字重复的那条。

**教训**：删测试时必须看**变异映射**，不能只看「这条断言看起来弱」。
`scripts/mutation-check.py` 的 `"test"` 字段就是一张「哪个文件守哪条不变量」的地图：
它没引用的文件（`runtime-correctness` / `data-integrity` / `team-service` / `tool-policy` /
`internal-api` / `member-dm` / `member-skills` / `governance-p0` / `member-decision`）才是安全删除区。
重跑后 **46/46 全部被捕获**。

### 失去的专属覆盖（用户明确接受）

1. manifest hash 的**两个输入维度**（工具声明的形状、工具 implementation）—— 排序与
   Provider 版本两维已恢复，形状/实现两维只剩「binding 变化」这一个代表
2. `serializeExternalWorkRef` 的单元往返（conversation 级的往返仍在 team-v1）
3. `resumeSession` 的「元数据本身也查不了」分支
4. 「部署放行但 Member 没绑定宿主工具 → 仍然拒绝」这一条（AND 的另一半还在）
5. 记忆的「不带 expectedVersion 强制覆盖」与「原子写不留 .tmp」两条
6. 原子写、`ensurePersonalKnowledgeBase` 幂等、FTS 引号注入、`listSources` 指向
7. `copilot.cancelTurn` 的 found=false 分支
8. 团队成员增删的形状约束（create 侧的约束仍在）
9. 模板目录不存在 / `enabled: false` 两个「不创建」分支
10. skill 名的路径分隔符校验（zip 条目穿越校验仍在）
11. `DefaultToolPolicy` 的 `privileged` 与 `external-write` 两个 describe 合并成一条路径
12. 哨兵的 `。` / `'…'` / `` `…` `` 三个包装变体、`<N` / `<NO_REP` 两个前缀中间态

### 交付门（全部通过）

`npm run typecheck` ✓ / 全量 **257 pass, 0 fail** ✓ / `npm run build` ✓ /
`node scripts/smoke-work-management.mjs` ✓ / `python3 scripts/mutation-check.py` **46/46** ✓ /
全量套件连跑 **12 轮 0 失败**（无新增 flake）。

恢复方式：本文件在 git 历史的本提交可查。
